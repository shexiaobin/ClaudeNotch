ClaudeNotch — 灵动岛风格 AI 编码通知（macOS 11+，Apple Silicon）

【安装步骤】
1. 将 ClaudeNotch.app 拖到「应用程序」文件夹（或任意固定位置，之后不要移动）。
2. 双击运行「Install Hooks.command」（首次可能需在右键菜单中选「打开」）。
3. 从「应用程序」或 Spotlight 启动 ClaudeNotch（菜单栏出现 ◉ 图标）。
4. 重启 Cursor；Claude Code 在下次会话时会使用新 hooks。

【系统要求】
• Apple Silicon (arm64)
• Python 3（系统自带即可）

【说明】
• 若仅把 .app 拷走而未运行 Install Hooks，Claude/Cursor 不会连到灵动岛。
• 移动 ClaudeNotch.app 后请重新运行一次 Install Hooks.command。
• 首次打开若被 macOS 拦截，请：系统设置 → 隐私与安全性 → 仍要打开。

【源码与许可】
见项目内 LICENSE / README。
